name = "MongrelDB"
uuid = "a3b1c2d4-e5f6-7890-abcd-ef0123456789"
authors = ["Visorcraft <noreply@visorcraft.com>"]
version = "0.60.2"

[deps]
# Sockets is a Julia standard library (not part of Base), so it must be listed
# here to satisfy `import Sockets`. JSON is vendored in src/JSON.jl, so there
# are no external/registry package dependencies to install.
Sockets = "6462fe0b-24de-5631-8697-dd941f90decc"

[compat]
julia = "1.9"

[extras]
Test = "8dfed614-e22c-5e08-85e1-d05778033a6d"

[targets]
test = ["Test"]
